#' @param destfile either character value indicating the name of the file
#' or `NULL` (the default), in which case the file name is constructed from
#' the other parameters of the function call, so that subsequent calls with
#' the same parameters will yield the same result; this is useful
#' for caching.

