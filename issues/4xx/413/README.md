## BD6901494_352.nc

[Note](Note): no adjusted pressure, so that won't copy over (etc).

```
ncdump BD6901494_352.nc | grep "float.*ADJ"
	float DOWN_IRRADIANCE380_ADJUSTED(N_PROF, N_LEVELS) ;
	float DOWN_IRRADIANCE380_ADJUSTED_ERROR(N_PROF, N_LEVELS) ;
	float DOWN_IRRADIANCE412_ADJUSTED(N_PROF, N_LEVELS) ;
	float DOWN_IRRADIANCE412_ADJUSTED_ERROR(N_PROF, N_LEVELS) ;
	float DOWN_IRRADIANCE490_ADJUSTED(N_PROF, N_LEVELS) ;
	float DOWN_IRRADIANCE490_ADJUSTED_ERROR(N_PROF, N_LEVELS) ;
	float DOWNWELLING_PAR_ADJUSTED(N_PROF, N_LEVELS) ;
	float DOWNWELLING_PAR_ADJUSTED_ERROR(N_PROF, N_LEVELS) ;
	float CHLA_ADJUSTED(N_PROF, N_LEVELS) ;
	float CHLA_ADJUSTED_ERROR(N_PROF, N_LEVELS) ;
	float BBP700_ADJUSTED(N_PROF, N_LEVELS) ;
	float BBP700_ADJUSTED_ERROR(N_PROF, N_LEVELS) ;
	float CDOM_ADJUSTED(N_PROF, N_LEVELS) ;
	float CDOM_ADJUSTED_ERROR(N_PROF, N_LEVELS) ;
```

## BD6901494_352D.nc

Note: no adjusted pressure, so that won't copy over (etc).

```
ncdump BD6901494_352D.nc | grep "float.*ADJ"
	float DOWN_IRRADIANCE380_ADJUSTED(N_PROF, N_LEVELS) ;
	float DOWN_IRRADIANCE380_ADJUSTED_ERROR(N_PROF, N_LEVELS) ;
	float DOWN_IRRADIANCE412_ADJUSTED(N_PROF, N_LEVELS) ;
	float DOWN_IRRADIANCE412_ADJUSTED_ERROR(N_PROF, N_LEVELS) ;
	float DOWN_IRRADIANCE490_ADJUSTED(N_PROF, N_LEVELS) ;
	float DOWN_IRRADIANCE490_ADJUSTED_ERROR(N_PROF, N_LEVELS) ;
	float DOWNWELLING_PAR_ADJUSTED(N_PROF, N_LEVELS) ;
	float DOWNWELLING_PAR_ADJUSTED_ERROR(N_PROF, N_LEVELS) ;
	float CHLA_ADJUSTED(N_PROF, N_LEVELS) ;
	float CHLA_ADJUSTED_ERROR(N_PROF, N_LEVELS) ;
	float BBP700_ADJUSTED(N_PROF, N_LEVELS) ;
	float BBP700_ADJUSTED_ERROR(N_PROF, N_LEVELS) ;
	float CDOM_ADJUSTED(N_PROF, N_LEVELS) ;
	float CDOM_ADJUSTED_ERROR(N_PROF, N_LEVELS) ;
```

